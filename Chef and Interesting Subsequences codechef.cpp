#include <iostream>
#include <vector>
using namespace std;

int main() {
	int T;
	cin >> T;
	while (T--) {
		int n, k;
		cin >> n >> k;
		vector<int> v;
		for (int i = 0; i < n; i++) {
			int c;
			cin >> c;
			v.push_back(c);
		}
		int sum = 0;
		for (int i = 0; i < n; i++) {
			for (int j = i; j < k; j++) {
				sum = sum + j;
				if(sum=sum+)
			}
			if

		}


	}
}